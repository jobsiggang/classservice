FROM node:18-alpine

WORKDIR /app

# 의존성 파일 복사
COPY package*.json ./

# 모든 의존성 설치 (빌드를 위해)
RUN npm ci

# 소스 복사
COPY . .

# TypeScript 빌드
RUN npm run build

# 프로덕션 의존성만 재설치
RUN npm ci --omit=dev

# Auth 서비스만 실행
EXPOSE 3001
CMD ["node", "dist/auth/server.js"]
